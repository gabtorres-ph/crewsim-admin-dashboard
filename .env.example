# Server-only Core API origin (do not include the /api path).
CORE_API_URL=http://localhost:8000

# Public URL embedded into the client bundle during the Docker build.
NEXT_PUBLIC_DOCUMENTATION_URL=https://example.com/docs
